import { style } from '@vanilla-extract/css';

export const overlayStyles = style({
  position: 'fixed',
  top: 0,
  right: 0,
  bottom: 0,
  width: '100%',
  maxWidth: '40rem',
  background: '#e6e6e6',
});
